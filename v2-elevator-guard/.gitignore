# ---------------------------------------------------------------- 密钥
# 里面是 API Key 和本机路径，绝对不要提交
.env
backend/.env
*.local

# ---------------------------------------------------------------- 运行产物
# 上传的原始视频、可视化视频、关键帧、PDF 报告、SQLite 数据库
backend/data/
data/
*.db
*.sqlite3

# ---------------------------------------------------------------- 模型权重
# 体积大且通常是私有资产。YOLO11n-Pose 首次运行会自动下载。
models/
*.pt
*.pth
*.onnx
*.engine

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# ---------------------------------------------------------------- IDE
.idea/
.vscode/

# ---------------------------------------------------------------- 其他
# ultralytics 在配置目录不可写时会在工作目录下生成这个目录
Ultralytics/
# 本地验证脚本的产物
_verify/
